# Отчёт о тестировании — Этап 5

Дата: 09.09.2026
База: Этап 4 (коммит 73a360f) + правки Этапа 5
Среда: mock-окружение tests/ (Python 3), без AutoCAD

## Итог

**ИТОГ: 24/24 PASS, 0 FAIL/ERROR**

### Группа 6 — Регрессия (выполняется первой)

- ✅ 6.1 Регрессия: Фасонка через диспетчер — PASS
- ✅ 6.2 Регрессия: Подсистема через диспетчер — PASS

### Группа 1 — Одиночные блоки / граничные случаи

- ✅ 1.1 Пустой чертёж — PASS
- ✅ 1.2 Один блок, размеры с припуском — PASS
- ✅ 1.3 Регрессия счётчика (2 одинаковых блока) — PASS
- ✅ 1.4 Регрессия сортировки (526 обязано быть перед 1226) — PASS
- ✅ 1.5 Нулевой размер пропускается — PASS
- ✅ 1.6 Регрессия trim слоёв (пробел после запятой) — PASS

### Группы 2 и 3 — Множественный выбор, слои, предвыбор

- ✅ 2.1 Несколько размеров на одном слое — PASS
- ✅ 3.1 Агрегация через слои (одинаковые блоки) — PASS
- ✅ 3.2 Фильтр по заданному слою — PASS
- ✅ 3.3 Предвыбор (pickfirst): использован и очищен — PASS

### Группа 4 — Диспетчер EXTRACTION

- ✅ 4.1 Отмена диалога EXTRACTION — PASS
- ✅ 4.2 Несуществующая задача — PASS
- ✅ 4.3 Отсутствующий модуль Заполнения — PASS
- ✅ 4.4 Незагруженный модуль (Облицовка) — PASS
- ✅ 4.5 Полный путь: диалог → Заполнение → запуск — PASS
- ✅ 4.6 Кнопка «Раскрой хлыста»: вход c:cutline разрешается — PASS
- ✅ 4.7 Кнопка «Раскрой листа»: вход c:cutsheet разрешается — PASS

### Группа 5 — Экспорт и сохранение

- ✅ 5.1 Экспорт XLS (подробный, путь по умолчанию) — PASS
- ✅ 5.2 Экспорт XLS (краткий) — PASS
- ✅ 5.3 Экспорт GAL — PASS
- ✅ 5.4 Занятый файл → ошибка, повтор после освобождения — PASS
- ✅ 5.5 Команда ZAPOLNENIE целиком — PASS

## Что покрыто

- задачи «Фасонка» и «Подсистема» через диспетчер `run-task` (регрессия);
- агрегация «Заполнения»: счётчик количества, числовая сортировка,
  trim слоёв после запятой (регрессии трёх исправленных дефектов);
- фильтр по слоям, склейка позиций через слои, предвыбор;
- диспетчер: отмена, несуществующая задача, отсутствующие модули,
  полный путь «Заполнения», кнопки «Раскрой хлыста»/«Раскрой листа»
  (регрессия дефекта «неверная функция: CUTLINE-MAIN»);
- экспорт XLS (подробный/краткий) и GAL, путь по умолчанию,
  занятый файл (fallback на CSV), интерактивная команда ZAPOLNENIE.

Прогон: `python3 tests/run_stage5_tests.py` (см. ЗАПУСК-ТЕСТОВ.md).

---

# Прогон 3 (после аудита, 10.09.2026)

Полный регресс после применения всех исправлений аудита
(`docs/AUDIT-ЭТАП5-+.md`): старые 24 теста + 11 новых регрессий.

**ИТОГ: 35/35 PASS.**

### Новые регрессии аудита

- ✅ 1.7 trim слоёв в c:fasonka (A2) — PASS
- ✅ 1.8 trim слоёв в c:subsystem (A2) — PASS
- ✅ 3.4 Предвыбор сохраняется при ошибке фильтрации (C3) — PASS
- ✅ 5.6 Фасонка: занятый XLS → fallback CSV реально создаётся (A1) — PASS
- ✅ 5.7 RELOAD перезагружает zapolnenie.lsp (A3) — PASS
- ✅ 7.1 FFD: деталь длиннее хлыста не размещается (A4) — PASS
- ✅ 7.2 c:cutline end-to-end: оверсайз → 0 хлыстов, итоги и CSV корректны (A4/A5) — PASS
- ✅ 7.3 CUTLINE: имя CSV по чертежу + сообщение при ошибке записи (B3/D3) — PASS
- ✅ 7.4 MLINE: делитель = полосы стиля DXF 71 (B1) — PASS
- ✅ 7.5 su-get-length: точное совпадение «ДЛИНА» приоритетно, иначе подстрока (B4) — PASS
- ✅ 7.6 clean-name: снимает только префикс «Железо » (B2) — PASS

Поведенческие замечания:

- kerf-модель «N×kerf» (отход уменьшается при каждой уложенной детали) —
  задокументировано как сознательное ограничение оценки КПД (D1);
- «зависший» undo-mark: c:cutline теперь оборачивает тело в `*error*` —
  CMDECHO восстанавливается и undo-группа закрывается при любом выходе (A5).

---

# Прогон 4 (доработки CUTLINE по замечаниям, 10.09.2026)

**ИТОГ: 37/37 PASS** (24 базовых + 13 регрессий аудита и доработок).

Доработано в `Extraction/cutline.lsp`:

- Сохранение результата — **XLS первичен** (`<имя чертежа> Раскрой хлыстов.xls`,
  формат XML Spreadsheet, как у остальных модулей), **CSV — только резерв**
  при занятом XLS (`n1-save-nest` → `n1-write-xls` → `n1-write-csv`).
- Неразмещённые детали (> хлыста) отображаются: в консоли (агрегированное
  «ВНИМАНИЕ…»), в XLS/CSV (секция «НЕРАЗМЕЩЕННЫЕ ДЕТАЛИ»), в таблице AutoCAD
  (секция красным, рамка расширяется под секцию).
- Список длин в ячейке «Детали» — через «2000 + 2000»: Excel больше
  не склеивает «2000 2000» в число с разрядами.
- Буква Ё исключена из всех строк модуля (шрифт таблицы её не рисует);
  добавлен тест 7.7.
- Kerf-граница: деталь, для которой p + kerf > stock, тоже не размещается
  (раньше давала отход < 0 при длине ровно в хлыст).

Новые/изменённые тесты: 7.1 (ffd+kerf), 7.2 (смешанный набор → XLS),
7.2b (все детали длинные → файл всё равно сохраняется), 7.3 (XLS→CSV резерв,
имя файла), 7.7 (нет Ё). Остальные регрессии без изменений.

---

# Прогон 5 (синхронизация с main d52ee07, 10.09.2026)

Ветка перебазирована на main (d52ee07 «Раскрой хлыстов: окно параметров»),
харнесс адаптирован к новым контрактам. **ИТОГ: 37/37 PASS.**

Найденные в main реальные дефекты (исправлены в коде):

- **zapolnenie-aggregate**: записи `(key name h w count)` сортировались
  компаратором `(name h w count)` → (car a) был key, порядок снова стал
  СТРОКОВЫМ («1226» перед «526») — возврат дефекта №2. Исправлено:
  ключ отрезается ДО сортировки.
- **n1-cutline-dialog**: `(vl-catch-all-apply '(lambda () ...))` вызван
  без списка аргументов → диалог параметров не открывался бы и в AutoCAD.
  Исправлено: добавлен `nil` вторым аргументом.
- **n1-ffd**: утрачен kerf-guard (p + kerf > stock → отрицательный отход) — возвращён.
- **su-select-inserts**: утрачена задержка очистки предвыбора до успешной
  фильтрации — возвращён флаг preset-used.
- **su-get-length**: утрачен двухпроходный поиск (точное «ДЛИНА» → подстрока) — возвращён.
- **cutline.lsp**: буквы Ё/ё снова появились — заменены на Е/е (тест 7.7).

Адаптации харнесса-под-новые-контракты-main (не баги): диалог параметров
CUTLINE (`dialog_script`), выбор только LINE/MLINE, новое сообщение
«Заполнение: обработано N блоков, общая площадь X м2» (десятичная запятая),
`ssadd` мока — мутация набора на месте (семантика AutoLISP), библиотечный
DISTANCE, вычисление символа функции как значения (type foo) → SUBR.

---

# Прогон 6 (синхронизация с main 96680e1 «Этап 2.4», 11.09.2026)

main переписан (history squash): новый срез — один коммит 96680e1
«диалог с 4 радиокнопками: LINE / MLINE / Динамические блоки / Все типы».
Ветка пересинхронизирована, харнесс прошёл. **ИТОГ: 37/37 PASS.**

Повторно применённые исправления (не вошли в новый main):

- zapolnenie-aggregate: ключ ДО сортировки (иначе «1226» < «526» строкой);
- n1-cutline-dialog: `nil`-arglist у `(vl-catch-all-apply '(lambda () ...))`;
- n1-ffd: kerf-guard (p + kerf > stock);
- su-select-inserts: очистка предвыбора после успешной фильтрации;
- su-get-length: два прохода (точное «ДЛИНА» → подстрока);
- cutline.lsp: буквы Ё/ё → Е/е.

Новый дефект main, найденный харнессом (исправлен):

- **cutsheet-main**: диспетчер передаёт 1 аргумент `(слои)`, а заглушка
  принимала 0 → «Модуль CUTSHEET не загружен или ошибка выполнения»
  ложно печаталось при нажатии «Раскрой листа». `cutsheet-main` теперь
  принимает аргумент (паттерн cutline-main).

## Прогон 7 — 11.09.2026 (main 96680e1 + разделитель « + »)

При аудите замечаний к main обнаружено, что в 96680e1 потеряно требование
раунда-2: `n1-list-to-str` соединял длины деталей пробелом (Excel при открытии
CSV «сливает» `2000 2000` в число 20002000). Функция используется в XLS
(ячейка «Состав хлыста»), CSV и консольном отчёте.
Исправлено: разделитель « + » (ASCII, CP1251-безопасно).

Результат: **37/37 PASS, 0 FAIL/ERROR.**
