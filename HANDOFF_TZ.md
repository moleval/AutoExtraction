# AutoExtraction — ТЗ и передача контекста (актуализация: сессия 2, 2026-09-23)

Репозиторий: `moleval/AutoExtraction`. Рабочая ветка сессии: `arena/01a0cef7-autoextraction`
(форк от `4d0b7b9`; контент дерева байт-в-байт равен `8d8078d`).

**Статус одной строкой:** Этапы 2, 3, 4 сделаны и слиты (PR #1). Этап 5: П1 готов; **П2 п.1 (метки bbox/wrap-block) выполнен и замерено: `CUTSHEET:bbox` 2578 мс, `CUTSHEET:wrap-block` 1235 мс, `CUTLINE:wrap-block` 609 мс — «белое пятно» оказалось главной статьёй всего запуска**. Приоритеты П2 переставлены (раздел 6): П2.2 bbox (A/B без `vla-update`) → П2.3 wrap (под-метки → по числам) → П2.4 draw-layout микро.

---

## 1. Где сейчас рабочая копия (актуализировано в сессии 2)

Инцидент 2026-09-23 15:29 (сессия 1): песочница агента была пересоздана, GitHub из неё был недоступен (SSL/TLS), мастер-копией объявлялся ПК пользователя `D:\autoextraction`, HEAD = `8d8078d`.

**Сессия 2 (та же дата, позднее): новая песочница — всё восстановлено и проверено.**

- Клон был shallow (глубина 1) от `4d0b7b9`; полная история вытянута с GitHub (`fetch --unshallow`, 177 коммитов, базовый `73a360f` на месте).
- `4d0b7b9` = merge коммитов `e01b2cf` (merge PR #1 в `main`) и `8d8078d` (П1). **Tree-hash HEAD (`80aefcc…`) идентичен tree `8d8078d` — контент байт-в-байт равен мастер-копии сессии 1.**
- **GitHub из новой песочницы доступен.** Удалённое состояние: `main` = `4d0b7b9` (merge PR #1 в истории — **отката удалёнки НЕ было**), ветка `arena/01a08717-autoextraction` жива, tip = `8d8078d`.
- Ветка сессии фиксирована платформой: `arena/01a0cef7-autoextraction`. Коммиты и пуш — только в неё; старую ветку `arena/01a08717-autoextraction` не трогаем (остаётся архивной точкой `8d8078d`).

Следовательно: мастер-копия теперь — ветка `arena/01a0cef7-autoextraction`. Страховочная ветка `backup/stage4-5-2026-09-23` на ПК пользователя остаётся полезной, но критичность снята.

---

## 2. История коммитов (что и в каком порядке сделано)

| SHA | Содержание |
|---|---|
| `73a360f` | база ветки (до сессии 1) |
| `b086083` | **Этап 4 / U1**: примитивы XLS в `common/excel-utils.lsp` + `tests/u1-selftest.lsp` |
| `ceef5e6` | `u1-selftest.lsp` UTF-8 → cp1251 |
| `09c2330` | **U2a**: CUTLINE `n1-write-xls` на примитивы; `tests/u2-xlsdiff.lsp` (семантический diff) |
| `3a3e9d9` | `u2-xlsdiff`: пустые `<Cell/>` исключены из потока токенов |
| `71d65c8` | **U2b**: CUTSHEET `cs-write-xls` на примитивы; `eu-doc-begin` +flavor FULL/BASIC; сканер v2 |
| `3b54ce7` | **U2c**: CLADDING — оба писателя (`cl-write-xls`, `cl-blocks-write-xls`) на примитивы |
| `da7cce7` | `u2-xlsdiff`: неоткрывшийся файл = громкий `[U2][FAIL]` + exit |
| `59dde20` | **U3**: `tu-unique-block-name` + счётчик редакций в RELOAD |
| `fec114f` | U3-fix: счётчик ловит оба стиля метки → 5 из 15 |
| `e01b2cf` | merge PR #1 → `main` (весь хвост Этапов 2–4) |
| `8d8078d` | **Этап 5 / П1**: `common/perf-utils.lsp` + метки в CUTLINE/CUTSHEET (tip ветки `arena/01a08717-autoextraction`) |
| `4d0b7b9` | merge `8d8078d` в `main` (текущий `main`; = база сессии 2) |

---

## 3. Что сделано и принято (Этапы 2–4)

- **Этап 2 (P0)** — защита и валидация входных данных: `common/validation-utils.lsp`, единый numeric/VARIANT-парсер, HARD-валидация, SOFT-sanity, лимиты (`*n1-max-parts*`, `*cs-max-parts*`, `*cs-max-placement-attempts*`), «невозможная деталь» до солвера.
- **Этап 3 (P1)** — транзакционность защитных операций: V8/V10/V9/V13, откат через UNDO, ActiveX-миграция.
- **Этап 4 (P1→P2)** — унификация вывода:
  - **U1** — примитивы XLS (`eu-doc-begin/end`, `eu-worksheet/end`, `eu-column`, `eu-row-begin/end`, `eu-cell`, `eu-cell-skip`); `ss:Index` ставится самим примитивом по курсору (класс дефекта `bde9b71` исключён структурно).
  - **U2** — все 4 XLS-писателя переведены на примитивы и **приняты диффами 1:1** с эталонами пользователя:
    - CUTLINE: 88 строк / 360 ячеек — ОК
    - CUTSHEET: 303 / 2259 — ОК
    - CLADDING блоки: 259 / 1775 — ОК
    - CLADDING полилинии: 35 / 196 — ОК
    - `u1-selftest` — OK
  - **U3** — общий генератор имён блоков `tu-unique-block-name` в `common/task-utils.lsp` (дубли `n1-`/`cs-` заменены делегатами) + счётчик редакций в RELOAD.

---

## 4. Этап 5, шаг П1 — сделано

- Новый модуль **`common/perf-utils.lsp`**: `pu-begin` / `pu-end` на `MILLISECS`, вывод `[PERF] КЛЮЧ: N мс`. Флаг **`*ae-perf-on*` по умолчанию nil** — при выключенном флаге метка стоит одну проверку условия (бесплатно), обычные прогоны молчат. Модуль добавлен в `reload.lsp` сразу после `task-utils.lsp`.
- Метки **CUTLINE**: `expand`, `ffd`, `xls`, `draw-header`, `draw-layout`, `draw-frame`. Редакция поднята до 6.
- Метки **CUTSHEET**: `collect`, `nest`, `xls`, `draw-layout`, `draw-summary` + отдельно все 6 вызовов солвера: `pack:area`, `pack:max-side`, `pack:min-side`, `pack:lastsheet:area`, `pack:lastsheet:area-rev`, `pack:lastsheet:min-side`. Редакция поднята до 16.
- Сверено в сессии 2: в CUTSHEET 11 меток `pu-begin`, в CUTLINE 6 — ровно как описано.

---

## 5. Профиль — ФАКТЫ (реальный чертёж пользователя, 2026-09-23, сессия 1)

**CUTSHEET**: 191 деталь, 18 листов, 743 примитива карты, полезный выход 85,2 %.

| Фаза | мс | Доля |
|---|---|---|
| `CUTSHEET:collect` | 63 | 4 % |
| `CUTSHEET:nest` (итого) | 703 | 39 % |
| ├ `pack:area` | 235 | |
| ├ `pack:max-side` | 203 | |
| ├ `pack:min-side` | 188 | |
| └ `pack:lastsheet:*` (15+16+15) | 46 | |
| `CUTSHEET:xls` | 32 | 2 % |
| **`CUTSHEET:draw-layout`** | **1094** | **61 %** |
| `CUTSHEET:draw-summary` | 109 | 6 % |

**CUTLINE**: 71 деталь, 24 хлыста, КПД 85,1 % — `expand` 0, `ffd` **0**, `xls` 31, `draw-header` 0, **`draw-layout` 282**, `draw-frame` 0.

**Выводы (критично, «на глаз» они обратны ожиданиям):**
1. **Солверы НЕ узкое место.** Весь `nest` — 0.7 с на 191 деталь; линейный `n1-ffd` — 0 мс. Оптимизировать `cs-score-rect` / `cs-pack-rects` **не нужно**: это цена качества раскладки.
2. **Узкое место — отрисовка.** 743 примитива ≈ **1.5 мс/шт**, `draw-layout` = 1.1 с (61 %). В CUTLINE то же: вся стоимость — 282 мс в `draw-layout`.
3. **Белое пятно, не покрытое метками**: bbox-обход примитивов и обёртка набора в блок через `-BLOCK` — суммарно оценочно сотни мс.

**Поправка к п.3 (сверка кода, сессия 2):** в ред. 16 `cs-entities-bbox` вызывается **ОДИН раз** (~стр. 1530, результат `bboxFact` перед рамкой). «Второй проход» после рамки — это пересборка набора `ssNew` через `ssadd`/`entnext` **без** `GetBoundingBox`/`vla-update` — копеечная операция, отдельной стоимости не несёт. Белое пятно = **один** bbox-обход + обёртка `-BLOCK`. В CUTLINE GetBoundingBox-обхода нет вовсе; там измерима только обёртка `-BLOCK`.

---

## 6. Этап 5, шаг П2 — план (пересмотрен по фактам сверки, сессия 2)

Приоритет по убыванию ожидаемого эффекта:

1. **Доснять метки** (безусловно первым; финальные названия — по результату замера, решено с пользователем):
   - `CUTSHEET:bbox` — вокруг единственного вызова `cs-entities-bbox` (метка одна: pass'ов два нет, см. раздел 5; если замер удивит — пересмотрим);
   - `CUTSHEET:wrap-block` — вокруг `cs-wrap-to-block` (внутри неё `_.-BLOCK` на ~1300; вызов после рамки ~1551);
   - `CUTLINE:wrap-block` — вокруг `_.-BLOCK` (~стр. 2142–2161);
   - редакции: CUTSHEET 16→17, CUTLINE 6→7; живой прогон на том же чертеже, таблица дополняет раздел 5.
   - Интерпретация: если `bbox` ≈ сотни мс — найден кандидат №2; если велик `wrap-block` — смотреть туда.

2. **`CUTSHEET:draw-layout` (1094 мс, 61 %) — главная цель оптимизации.** Уточнение стратегии после сверки кода: `command`-вызовов в отрисовке **нет, всё уже на `entmake`** — гипотеза «command → entmake ×3–10» снята. Остаются:
   - **2а. Кэш текстовых стилей:** per-вызовный `(tblsearch "STYLE" …)` в `cs-draw-text` / `cs-draw-text-bold` / `cs-draw-text-center` (~260+ вызовов за прогон) → вычислить один раз за прогон (глобаль с `(boundp)`-защитой);
   - **2б. Замер по классам примитивов** (SOLID-заливки с прозрачностью / LWPOLYLINE / TEXT) — понять, какой класс ест 1.5 мс/шт; прозрачность SOLID — подозреваемый на дорогую регенерацию;
   - **2в. Инварианты DXF-списков вынести из цикла** (одинаковые слой/цвет у однотипных примитивов), группировать однотипные entmake.
   - Жёсткое требование прежнее: **поведение-сохраняюще** — тот же состав, порядок, координаты, слои/цвета/стили/прозрачность примитивов.

3. **(Снят как отдельный пункт)** «двойной bbox» — проход один (раздел 5). Если замер `CUTSHEET:bbox` покажет >~200 мс — обсудить безопасное ускорение самого прохода (с гарантией «рамка не обрезает строки»); если десятки мс — не трогать вовсе.

4. **Прогресс-строка + отмена по ESC** (п.20 карты) — после П2, по договорённости: >700 примитивов и >1 с молчащего экрана. Юзабилити, не скорость, но в том же месте кода.

**Ожидание честное (после замера П2 п.1):** цели упорядочены фактами. Реалистично: `bbox` 2578 → ~300–800 (если A/B успешен), `wrap-block` 1235 → ~300–600, `draw-layout` 1109 → ~900–1000. Фаза карты CUTSHEET (layout+summary+bbox+wrap ≈ 5.0 с) → ~1.5–2.4 с; суммарный видимый запуск ~6.0 с → ~2.7–3.6 с.

**Кандидаты, забракованные профилем (без изменений):** `cs-score-rect`, `cs-get-used-parts`, `n1-ffd`. Алгоритмику раскроя не трогаем. Если после п.2 захочется «дожать» `nest` — сначала новые замеры.

---

## 7. Жёсткие правила и ограничения

**Из раздела «Чего НЕ делаем» (зафиксировано):**
- Не переписываем `fasonka`, солверы (FFD/MaxRects), не объединяем `*-main` в «универсальную» функцию.
- Не делаем бог-объект `utils.lsp`; механически не заменяем `append`/`foreach`.
- Не вводим десятки `*flag*`; не оборачиваем КАЖДУЮ строку в `vl-catch-all`.
- HARD-лимиты — только именованные переменные с дефолтами; SOFT-пороги не «чинят» значение автоматически (никаких «0.35 → 35»).
- **Алгоритмику раскроя не трогаем.** Перф-правки должны быть поведение-сохраняющими: тот же состав и порядок примитивов, те же координаты, те же XLS.

**Рабочие конвенции (выстраданы на практике):**
- **Кодировка всех `.lsp` — cp1251 (ANSI)**, AutoCAD читает ANSI. Патч-скрипты читают/пишут строго `cp1251`. Markdown-файлы репозитория — UTF-8.
- **Grep по кириллице** только через `iconv -f cp1251 -t utf-8 файл | grep …`, иначе 0 совпадений молча.
- **Проверка баланса скобок** после каждой правки — полным сканером с учётом строк, комментариев `;` и экранирования `\"` (в репо есть `tests/chkparens.lsp` и `tests/static_check.py` — прогонять после каждой правки).
- **Патч-скрипты идемпотентны**: проверка «уже пропатчено» + `assert count(old)==1` перед заменой. Скрипты держать в `/tmp`, новый на задачу.
- Один дефект/рефакторинг → один коммит с понятным сообщением.
- Единый формат ошибок: `[AutoExtraction][MODULE][KIND] текст` (KIND: `VALIDATION`, `ACTIVE-X`, `IO`, `GUARD`).

**Справочник по коду (строки сверены 2026-09-23 на `4d0b7b9`/`8d8078d` — точные):**
- `common/excel-utils.lsp` — примитивы U1. `eu-doc-begin (f [flavor])` :1001; flavor `"FULL"` (со стилями) и `"BASIC"`; мини-flavor CLADDING упразднён в пользу BASIC. `eu-column` поддерживает `+autofit`; `eu-row-begin` — `+attrs`; `ss:Formula`/`MergeAcross` — через attrs `eu-cell`; экранирование внутри `eu-cell`.
- `common/task-utils.lsp` — `tu-unique-block-name (base / n name)` :254: старт `n=0`, первое имя `(strcat base " " (itoa 0))`, подбор по `(tblsearch "BLOCK" name)`. **Семантику «первое имя с суффиксом 0» сохранять обязательно** (регресс имён блоков).
- `common/perf-utils.lsp` — `pu-begin`/`pu-end`, `*ae-perf-on*`, `*pu-marks*`. Вложенные одноимённые метки не поддерживаются.
- `reload.lsp` — списки модулей `*ae-reload-common-files*` / `*ae-reload-extraction-files*`, `ae-reload-load-file` (с chkparens-диагностикой), `ae-reload-rev-count` (ищет `"(ред. "` **или** `", ред. "`), глобальный счётчик `*ae-reload-run*`, строка сводки `RELOAD завершен (прогон #N).` + `Маркер редакции: K из M модулей.` **Новый модуль обязательно добавлять в список загрузки.**
- `Extraction/cutsheet.lsp` (ред. 16) — солвер: `cs-score-rect` :514, `cs-subtract-rect` :553, `cs-get-used-parts` :572, `cs-pack-rects` :578, `cs-nest` :665, `cs-optimize-last-sheet` :689; отрисовка: `cs-ensure-italic-style` :723, `cs-ensure-bold-style` :733, `cs-draw-line` :743, `cs-draw-rect` :749, `cs-draw-filled-rect` :772 (SOLID+прозрачность), `cs-draw-text` :806, `cs-draw-text-bold` :813, `cs-draw-text-center` :821 (везде per-вызовный `tblsearch "STYLE"`), `cs-draw-sheet-header` :840, `cs-draw-placement` :853, `cs-draw-summary` :871, `cs-entities-bbox` :1014 (`vla-update`+`GetBoundingBox` на каждый примитив), `cs-draw-frame` :1046, `cs-draw-layout` :1063; `cs-write-xls` :1114; `cs-unique-block-name` :1273 (делегат → `tu-unique-block-name`); `cs-wrap-to-block` :1277 (внутри `_.-BLOCK` ~:1300, сообщение «[wrap] Блок создан…» ~:1312); **единственный** вызов `cs-entities-bbox` :1530; вызов обёртки ~:1551. **Отрисовка целиком на `entmake`, `command`-вызовов нет.**
- `Extraction/cutline.lsp` (ред. 6) — `n1-unique-block-name` :167 (делегат), `n1-draw-frame` :395, `n1-draw-header` :422, `n1-ffd` :522, `n1-draw-layout` :1208, `n1-write-xls` :1480; сбор `ssNew` + `_.-BLOCK` ~:2142–2161 (защита от двойного INSERT, Шаг 4). GetBoundingBox-обхода нет.
- `Extraction/cladding.lsp` (ред. 30) — оба XLS-писателя на примитивах. Сообщение «Блоки облицовки не найдены.» (~991) — **легитимная** else-ветка `(if brec …)`, не дефект.
- `tests/u2-xlsdiff.lsp` — семантический diff XLS: ячейки `колонка|стиль|merge|значение`, игнорирует способ задания `ss:Index`; при неоткрывшемся файле — `[U2][FAIL]` + exit.
- `tests/u1-selftest.lsp` — самотест примитивов (должен быть OK).

**Ловушки (уроки, не повторять):**
- Diff-инструмент обязан падать на IO-ошибках **до** сравнения: из-за несуществующего пути однажды получили зелёный `0=0 [OK]` (оба потока пустые).
- Пустые ячейки-пропуски (`<Cell/>` вместо `ss:Index`) давали ложный +27 FAIL — исключены из потока.
- Спан-парсинг `</Cell>`: длина 7, off-by-one уже чинился.
- V13-контракт проверяется откатом через UNDO.

---

## 8. Протокол приёмки (обязателен после каждого шага)

> В сессии 2 AutoCAD есть только на ПК пользователя: агент готовит патчи и статические проверки (cp1251, скобки, `static_check.py`), живые прогоны и диффы выполняет пользователь и возвращает выводы.

1. `git pull` → `RELOAD`: модулей 16, маркер редакции, `CUTLINE.LSP (ред. 7)`, `CUTSHEET.LSP (ред. 17)`, `PERF-UTILS.LSP загружен.`
2. `(setq *ae-perf-on* T)` → прогнать **CUTSHEET на том же чертеже** (191 деталь / 18 листов) **дважды — в таблицу брать второй (тёплый) прогон** — и **CUTLINE** → снять таблицу мс, сравнить с разделами 5 / 5.1.
3. Неизменность результата: XLS-файлы байт-совместимы по смыслу — `u2-xlsdiff` против эталонов (CUTLINE 88/360, CUTSHEET 303/2259). Если правки касались только отрисовки — сверить визуально: та же карта, рамка не обрезает строки, блок создаётся и вставляется (`[wrap] Блок создан: да, ссылок: 0` / `после вставки: 1`).
4. Имена блоков-обёрток — как раньше: «Раскрой … N», «Раскрой листа … N» (регресс-тест `tu-unique-block-name`).
5. `(setq *ae-perf-on* nil)` после замеров.

---

## 9. Чек-лист старта — выполнено в сессии 2

1. ✅ Старт от контента `8d8078d` подтверждён tree-hash'ем; в базе `73a360f` заглушки не используются; полная история восстановлена.
2. ✅ Документ ТЗ показан агенту и актуализирован (этот файл; журнал — раздел 11; файл закоммичен в репозиторий).
3. ⏳ Раздел 6: п.1 (доснять метки) — следующий шаг; затем п.2; п.3 снят.
4. Приёмка (раздел 8) — после каждого шага; живые прогоны на ПК пользователя.

---

## 10. Открытые вопросы / решения

- **Ветка: решено.** Работа в `arena/01a0cef7-autoextraction`; `arena/01a08717-autoextraction` остаётся архивной (tip `8d8078d`); влить в `main` — PR-ом по готовности П2 (решает пользователь).
- **Именование bbox-метки: решаем по замеру** (договорено с пользователем): добавляется одна `CUTSHEET:bbox` соответственно фактической структуре кода; pass1/pass2 не вводим, пока живой замер не покажет иное.
- Остаток Этапа 5 (п.16 кэш динамических свойств блоков, п.17 лишние `ssget "_X"`, п.18 `entget`/ActiveX в горячих циклах) — брать только после П2 и только по замерам.

---

## 11. Журнал изменений ТЗ

- **2026-09-23, сессия 2** (эта ревизия):
  - Раздел 1 переписан: песочница восстановлена, GitHub доступен, отката удалёнки нет; мастер-копия — ветка `arena/01a0cef7-autoextraction` (контент = `8d8078d`, подтверждено tree-hash'ем `80aefcc…`).
  - Раздел 5: поправка — `cs-entities-bbox` вызывается один раз; «второй проход» — дешёвая пересборка набора без `GetBoundingBox`.
  - Раздел 6 пересмотрен: отрисовка уже на `entmake` (гипотеза «command→entmake» снята); добавлены пункты 2а–2в; бывший п.3 («двойной bbox») снят; ожидания пересчитаны честно (~1.4–1.6 с без риска).
  - Раздел 7: номера строк сверены и уточнены; отмечено отсутствие `command` в отрисовке CUTSHEET.
  - Раздел 9: пункты 1–2 закрыты; добавлено примечание о разделении труда агент/пользователь.
  - Файл перенесён из untracked в репозиторий (раньше жил только в песочнице сессии 1).
- **2026-09-23, П2 п.1 (выполнено):** метки `CUTSHEET:bbox` (вокруг единственного вызова `cs-entities-bbox`), `CUTSHEET:wrap-block` (внутри then-ветки обёртки, вокруг `cs-wrap-to-block`), `CUTLINE:wrap-block` (вокруг всего if-обёртки: `ssget "_X"`-счётчики + `_.-BLOCK` + вставка; метка закрывается на всех путях); редакции CUTSHEET 16→17, CUTLINE 6→7. Странная else-ветка `(tu-diag "BLOCK" …)` в CUTSHEET обнаружена, но сознательно НЕ тронута (вне зоны П2, поведение-сохранение). Ожидание от замера: `bbox` — сотни мс? `wrap-block` — неизвестно. static_check PASS, идемпотентность патча проверена.
