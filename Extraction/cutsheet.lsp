(defun c:CUTSHEET ()
  (princ "\nCUTSHEET: модуль в разработке.")
  (princ)
)

;; ============================================================
;; Обёртка для запуска из диспетчера EXTRACTION
;; ============================================================
(defun cutsheet-main (layers-from-caller /)
  ;; Диспетчер EXTRACTION передаёт выбранные слои (как и в cutline-main);
  ;; пока модуль — заглушка, аргумент принимаем для совместимости.
  (c:cutsheet)
)

(princ "\nCUTSHEET.LSP загружен. Команда: CUTSHEET")
(princ)