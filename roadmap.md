# Дорожная карта AutoExtraction: Этап 2+ («Защита → Унификация → Производительность»)

Зафиксировано: 2026-09-22, ветка `arena/01a08717-autoextraction` (тип `bde9b71`+).
Статус старта: стабилизация закрыта, техдолг = 0, алгоритмы раскроя (FFD/MaxRects) **не меняем**.

## Принципы этапа

1. **Алгоритмы раскроя не трогаем** — защита строится по периметру (вход → нормализация → валидация → sanity → алгоритм), риск регрессии минимален.
2. **Три разных слоя защиты, не смешивать:**
   - *Нормализация* — `«1500»/1500/VARIANT(1500) → 1500.0`;
   - *Валидация (HARD)* — технически невозможное отклоняется (`≤ 0`, NaN, нет геометрии);
   - *Sanity (SOFT)* — физически подозрительное (`width < 1 мм`, `width > 100000 мм`) — не молча чиним и не роняем: пропуск с предупреждением/отчётом.
3. **Лимиты — защитные, а не предметные.** Конкретные максимумы НЕ зашиваются вслепую: именованные переменные с дефолтом (`*n1-max-bar-length*` = 100000 мм, `*n1-max-parts*` = 5000 …), изменяемые.
4. **Один дефект → один коммит; один рефакторинг → один коммит.** Каждая задача таблицы — отдельный коммит с приёмкой (харнесс-гейты + живой прогон пользователя).
5. Сообщения об ошибках — единый формат: `[AutoExtraction][MODULE][KIND] текст` (KIND: `VALIDATION`, `ACTIVE-X`, `IO`, `GUARD`).

---

## Этап 2 (P0) — Защита и валидация входных данных

| ID | Задача | Состав | Приёмка |
|---|---|---|---|
| V1 | `common/validation-utils.lsp` | Предикаты: `tu-positive-number-p`, `tu-nonnegative-number-p`, `tu-positive-integer-p`, `tu-number-in-range-p`, `tu-string-nonempty-p`, `tu-valid-point-p`, `tu-valid-size-p`, `tu-valid-sheet-size-p`, `tu-valid-bar-length-p`, `tu-valid-kerf-p`. Загружается RELOAD'ом раньше модулей. | CHKALL зелёный; предикаты покрыты харнессом; модульные пороги — параметрами, не «вросшие». |
| V2 | Единый numeric/VARIANT parser | Один `tu-parse-number` (запятая→точка, VARIANT/ошибки, nil) — заменяет прямые `atof` в cutline (строки ~1007-1009), cutsheet (~399-401) и `(read (strcat "(" s ")"))` в extraction.lsp:384. Образец-прототип: `cs-parse-number` (cutsheet.lsp:111). | Ввод «abc»/«-500» в диалоге → отказ с сообщением на этапе OK, а не 0.0 в алгоритме. |
| V3 | Валидация размеров/площадей (диалоги) | Хлыст ∈ (0; max], рез ∈ [0; допустимо] и рез < хлыст, лист W/H ∈ (0; max] и рез < min(сторон), допуск ≥ 0. Отказ = остаться в диалоге с красной подсказкой. | Живой прогон: заведомо неверные значения отклоняются диалогом. |
| V4 | Валидация геометрии объектов (чертёж) | Единый «допуск» измерений: LWPOLYLINE — замкнута, площадь > 0, bbox существует, стороны > 0; INSERT — свойства «Длина»/«Ширина» числовые, HARD `> 0`, SOFT-диапазоны; аномалии — счётчик «проигнорировано N». | Прогон на мусорном объекте: модуль не падает, объект перечислен в отконтроле. |
| V7 | «Невозможная деталь» до солвера | CUTSHEET: деталь, не помещающаяся на лист ни в одной ориентации — сразу в «НЕРАЗМЕЩЕННЫЕ» с причиной («не помещается на лист 1500×3000»), а не в MaxRects. CUTLINE: деталь > хлыста уже отсекается — добавить в XLS причину. | Прогон: лист 1500×3000 + деталь 1500×3200 → ясный отчёт, солвер не вызывается впустую. |
| V5 | Лимит количества деталей | `*n1-max-parts*`/`*cs-max-parts*` (дефолт 5000): превышение → «Обнаружено N деталей. Обработка остановлена. Лимит: 5000. Проверьте выборку/слои.» | Защита от зависания при ошибочной выборке (×100 объектов). |
| V6 | Лимит итераций раскроя | Стражи в MaxRects/FFD: `MAX_PLACEMENT_ATTEMPTS`; при срабатывании — частичный честный результат: «Алгоритм остановлен ограничителем. Размещено: X/Y.» | Искусственный патологический кейс в песочнице: нет бесконечного цикла. |
| V11 | Единый диагностический протокол | Тумблер `*EXTRACTION-DIAGNOSTIC*` (дефолт nil). При T — строки `[SCAN]/[FILTER]/[VALIDATION]/[PACK]/[DRAW]/[BLOCK]` с количествами. В обычной работе — тишина. Вместе: единый формат сообщений об ошибках (принцип 5). | Вкл/выкл без правок кода; продуктивный вывод не изменился. |

## Этап 3 (P1) — Унификация защитных операций (транзакционность)

| ID | Задача | Состав | Приёмка |
|---|---|---|---|
| V8 | Единый guard системных переменных | Паттерн SAVE→MODIFY→WORK→RESTORE только для реально меняемых (`CMDECHO`, `OSMODE`, `CLAYER` …). Обёртка `ex-safe-main` для команд módulos. | Любой abort (ESC/ошибка) возвращает CMDECHO/OSMODE; проверено живьём. |
| V10 | Единый Undo-guard | `tu-undo-begin/end/cancel`; закрыть пробелы: blockrename (сейчас без маркеров — половинчатое переименование), диспетчер extraction.lsp. | Прерывание посреди массового переименования → откат целиком. |
| V9 | Единый контракт `safe-call` для ActiveX | Один API (`OK + value` / `ERROR + message`) для `vla-*` вместо разнобоя `vl-catch-all-apply`. OLD iteration: не оборачивать каждую строку — только внешние вызовы AutoCAD/ActiveX. | Список точек миграции; vla-InsertBlock — первый кандидат. |
| V13 | Транзакционность результата | Принцип «всё или ничего»: падение на 8-м листе откатывает полусозданный блок и временные объекты (UndoMark + U/ActiveX undo). | Искусственная ошибка в середине создания → в чертеже не остаётся обломков. |

## Этап 4 (P1→P2) — Унификация вывода

| ID | Задача | Состав | Приёмка |
|---|---|---|---|
| U1 | Примитивы XLS в `common/excel-utils` | `eu-cell` (колонка тикает сама — `ss:Index` руками задать нельзя → класс дефекта `bde9b71` невозможен), `eu-row-begin/end`, общий блок стилей. | xml-гейт: весь сгенерированный XLS проходит well-formedness + контракт колонок. |
| U2 | Миграция трёх собственных писателей | cladding (~450 строк XML, 18 стилей), cutline (251 write-line), cutsheet → на примитивы U1. Ожидание: −300…400 строк. Содержимое ячеек 1:1 (снапшот-сравнение XML до/после). | XLS всех пяти модулей открываются; diff-снапшоты содержимого совпадают. |
| U3 | Мелкая унификация | Генератор «следующий номер имени» («Раскрой Эксперимент N»), счётчик ред. в RELOAD — в common. | Без смены поведения. |

## Этап 5 (P2) — Производительность (только после Этапов 2–4)

15. Убрать реальные O(n²) места (найти профилировкой, не «на глаз»).
16. Кеш чтения динамических свойств блоков (сейчас повторные обходы).
17. Не делать повторные `ssget "_X"` там, где набор уже есть.
18. Сократить `entget`/ActiveX-вызовы в горячих циклах.
19. MaxRects: профилировка → точечная оптимизация (алгоритмика — только по фактам).
20. Графика: пакетное создание примитивов; прогресс-строка + отмена по ESC в тяжёлом нэстинге (юзабилити, не скорость).

---

## Чего НЕ делаем (оба списка, зафиксировано)

- Не переписываем `fasonka`, солверы (FFD/MaxRects), не объединяем `*-main` в «универсальную» функцию.
- Не делаем бог-объект `utils.lsp`; механически не заменяем `append`/`foreach`.
- Не вводим десятки `*flag*`; не оборачиваем КАЖДУЮ строку в `vl-catch-all`.
- HARD-лимиты не зашиваем вслепую — только именованные переменные с дефолтами.
- SOFT-пороги не «чинят» значение автоматически (никаких «0.35 → 35»).

## Зависимости и порядок старта

```
V1 ─┬─> V2 ──> V3 (диалоги)
    └─> V4 (геометрия) ──> V7 (невозможные детали)
V5 (лимит деталей) — независим, можно сразу
V6 (лимит итераций) — после V5, нужны стражи с общим «GUARD»-протоколом (V11, лёгкая часть)
Этап 3: V8 → V10 → V9 → V13 (каждый строится на предыдущем; ex-safe-main интегрирует V8+V10)
Этап 4: U1 → U2 → U3
Этап 5: после стабилизации 2–4
```

**Предлагаемый первый рабочий коммит:** V1 + V2 (они одна волна: предикаты + парсер), приёмка — диалог CUTLINE с «abc»/«-500» отклоняется красной подсказкой, CHKALL зелёный. Дальше — V3, V4, V7 одной волной («валидация везде»), потом V5/V6.
